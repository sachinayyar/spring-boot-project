apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: source-pvc
  namespace: pipeline-demo
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi

